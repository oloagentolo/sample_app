class UsersController < ApplicationController
  def new
    @title = "Sser.namign up"
    @user = User.find(params[:id])
  end

  def show
    @user = User.find(params[:id])
    @title = @u
  end
end
